packages:
  - 'packages/*'

# Разрешённые postinstall-скрипты.
# esbuild — транзитивная зависимость tsx и vitest.
# argon2 — нативный биндинг для argon2id хеша, обязательный.
allowBuilds:
  argon2: true
  esbuild: true
